[gd_scene format=3 uid="uid://cgoxenu3igl6p"]

[ext_resource type="Texture2D" uid="uid://cy7utpht5os4o" path="res://ui/box/1.png" id="1_fys3n"]
[ext_resource type="AudioStream" uid="uid://bliy8igu7h36b" path="res://sfx/box hit.wav" id="2_oy2pg"]

[sub_resource type="GDScript" id="GDScript_fys3n"]
script/source = "extends Node2D

@onready var sprite: Sprite2D = $\"1\"
@onready var static_body_2d: StaticBody2D = $StaticBody2D
@onready var audio_stream_player_2d: AudioStreamPlayer2D = $AudioStreamPlayer2D

signal hit_object(strength)

# === SETTINGS ===
var grid_x := 6
var grid_y := 2

var explosion_strength := 1000.0
var fall_gravity := 400.0
var fade_speed := 1.0

var destroy_delay := 0.8  # ⏱ time before removing box

var pieces := []
var broken := false


func _ready() -> void:
	randomize()
	add_to_group(\"breakable\") # ✅ IMPORTANT


func _process(delta: float) -> void:
	if broken:
		update_pieces(delta)


func _on_body_entered(body: Node2D) -> void:
	if body.has_method(\"car\"):
		audio_stream_player_2d.play()
		if broken:
			return

		broken = true

		# 🔥 impact strength for camera shake
		var strength := 1.0
		if body is RigidBody2D:
			strength = body.linear_velocity.length() / 300.0

		break_sprite()

		# remove collision instantly
		if static_body_2d:
			static_body_2d.queue_free()

		emit_signal(\"hit_object\", strength)

		# ⏱ remove full box after animation
		await get_tree().create_timer(destroy_delay).timeout
		queue_free()


# === BREAK ===
func break_sprite():
	sprite.visible = false

	var tex = sprite.texture
	var size = tex.get_size()

	var piece_w = size.x / grid_x
	var piece_h = size.y / grid_y

	for x in range(grid_x):
		for y in range(grid_y):
			var p = Sprite2D.new()
			p.texture = tex
			p.region_enabled = true

			p.region_rect = Rect2(
				x * piece_w,
				y * piece_h,
				piece_w,
				piece_h
			)

			var offset = Vector2(
				x * piece_w - size.x / 2 + piece_w / 2,
				y * piece_h - size.y / 2 + piece_h / 2
			)

			p.position = sprite.position + offset
			add_child(p)

			# 🔥 RANDOM SPREAD
			var dir = Vector2(
				randf_range(-1.0, 1.0),
				randf_range(-1.2, -0.3)
			).normalized()

			var speed = randf_range(120.0, explosion_strength)
			var spin = randf_range(-8.0, 8.0)
			var drift = randf_range(-30.0, 30.0)

			pieces.append({
				\"node\": p,
				\"vel\": dir * speed,
				\"spin\": spin,
				\"drift\": drift
			})


# === UPDATE ===
func update_pieces(delta):
	for i in range(pieces.size() - 1, -1, -1):
		var piece = pieces[i]
		var p = piece[\"node\"]

		if not is_instance_valid(p):
			pieces.remove_at(i)
			continue

		var vel = piece[\"vel\"]

		# sideways drift
		vel.x += piece[\"drift\"] * delta

		# movement
		p.position += vel * delta

		# gravity
		vel.y += fall_gravity * delta
		piece[\"vel\"] = vel

		# rotation
		p.rotation += piece[\"spin\"] * delta

		# fade out
		p.modulate.a -= fade_speed * delta

		if p.modulate.a <= 0:
			p.queue_free()
			pieces.remove_at(i)
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_vt8vk"]

[sub_resource type="RectangleShape2D" id="RectangleShape2D_j7qbe"]

[node name="box" type="Node2D" unique_id=859864415]
light_mask = 3
visibility_layer = 3
script = SubResource("GDScript_fys3n")

[node name="1" type="Sprite2D" parent="." unique_id=319818173]
position = Vector2(2.0000002, 39.000008)
scale = Vector2(0.755, 0.755)
texture = ExtResource("1_fys3n")

[node name="Area2D" type="Area2D" parent="." unique_id=1987069841]

[node name="CollisionShape2D" type="CollisionShape2D" parent="Area2D" unique_id=568993190]
scale = Vector2(26.781631, 26.781631)
shape = SubResource("RectangleShape2D_vt8vk")

[node name="StaticBody2D" type="StaticBody2D" parent="." unique_id=625599494]

[node name="CollisionShape2D" type="CollisionShape2D" parent="StaticBody2D" unique_id=526642366]
position = Vector2(-3, 34)
scale = Vector2(19.82084, -19.336184)
shape = SubResource("RectangleShape2D_j7qbe")

[node name="AudioStreamPlayer2D" type="AudioStreamPlayer2D" parent="." unique_id=1958131670]
stream = ExtResource("2_oy2pg")

[connection signal="body_entered" from="Area2D" to="." method="_on_body_entered"]
